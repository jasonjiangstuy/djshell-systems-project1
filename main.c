// Used for launching the shell and testing

#include "shell.h"

int main() {
    // printf("TEST\n");

    // printf("%s\n", strip(hi));
    // printf("%s\n", strip("   ls -a -l   \n"));
    // parse_args()
    launch_shell();
    return 0;
}
