// Used for launching the shell and testing

#include "shell.h"

int main() {
    // printf("TEST\n");
    // printf("%s\n", stripOneWord("   ls     \t\n"));
    // char * hi = "   ls -a -l   \n";
    // parse_args()
    launch_shell();
    return 0;
}
